<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context="com.example.fernana6.sqliteserverr.MainActivity">

    <EditText
        android:id="@+id/mailLoggin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Email"/>


    <EditText
        android:id="@+id/passwordLoggin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        app:layout_constraintTop_toBottomOf="@+id/mailLoggin"/>

    <Button
        android:id="@+id/loggearBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/passwordLoggin"
        android:text="Aceptar"
        android:textAllCaps="false"
        android:onClick="onClick"/>

    <Button
        android:id="@+id/registrarBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@+id/loggearBtn"
        app:layout_constraintTop_toBottomOf="@+id/passwordLoggin"
        android:text="Registrarse"
        android:textAllCaps="false"
        android:onClick="onClick"/>




</android.support.constraint.ConstraintLayout>
